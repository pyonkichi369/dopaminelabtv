{
  "name": "dopaminelabtv",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-og.mjs && (python3 scripts/generate-og-ja.py || echo 'JA OGP skipped (Pillow/font unavailable — use pre-built images)') && astro build",
    "preview": "astro preview",
    "new-post": "node scripts/new-post.mjs"
  },
  "dependencies": {
    "astro": "^4.15.0",
    "@astrojs/mdx": "^3.1.0"
  }
}
